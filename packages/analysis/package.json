{
  "name": "@fraudasaurus/analysis",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "python -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "extract": ".venv/bin/python src/extract.py"
  }
}
